# Left (central) half only.
# Disable the OLED on the central half: enabling it hangs this half (central-only bug
# in the petejohanson power-domains fork). The peripheral (right) half's display is
# unaffected and stays enabled.
CONFIG_TUKON_DISPLAY=n
