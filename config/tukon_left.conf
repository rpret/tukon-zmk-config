# Left (central) half only.
# Disable the OLED on the central half: enabling it hangs boot BEFORE USB enumeration.
# USB-logging diagnostic confirmed the identical firmware works on the right
# (peripheral) half, so this is a left-side OLED/I2C/MOSFET hardware fault — not firmware.
# Re-enable only after the left OLED hardware is repaired.
CONFIG_TUKON_DISPLAY=n
